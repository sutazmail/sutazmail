# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!deploy/.env.example

# generated prisma client
/src/generated

# runtime data (postgres bind mounts, mail volumes)
/data
deploy/data

# local operator notes / internal roadmap — NEVER commit (contain live credentials,
# NAS SSH access patterns, cert paths — see prepublish audit 2026-07-03)
Push and Deploy this into GitHub to.txt
ENTERPRISE-PLAN.md
*.local.txt

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
